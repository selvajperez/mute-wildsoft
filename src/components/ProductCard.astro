---
import type { CatalogProduct } from "../lib/sheets";
import { formatMoney } from "../lib/currencies";

interface Props {
  product: CatalogProduct;
}

const { product } = Astro.props;

const estadoClase: Record<string, string> = {
  "Agotado": "estado-agotado",
  "Últimas unidades": "estado-poco",
  "Disponible": "estado-ok",
};

const puedeAgregar = product.estado !== "Agotado";
---

<article
  class="card"
  data-nombre={product.producto.toLowerCase()}
  data-categoria={product.categoria}
>
  <div class="foto">
    <button
      type="button"
      class="foto-zoom"
      data-foto={product.fotoUrl}
      data-nombre={product.producto}
      aria-label={`Ver imagen ampliada de ${product.producto}`}
    >
      <img src={product.fotoUrl} alt={product.producto} loading="lazy" />
    </button>
    <span class={`estado ${estadoClase[product.estado]}`}>{product.estado}</span>
  </div>
  <div class="info">
    <p class="categoria">{product.categoria}</p>
    <h3>{product.producto}</h3>
    <div class="fila-inferior">
      <p class="precio">{formatMoney(product.precio, product.moneda)}</p>
      {puedeAgregar && (
        <button
          type="button"
          class="btn-agregar"
          aria-label={`Agregar ${product.producto}`}
          data-id={product.id}
          data-nombre={product.producto}
          data-precio={product.precio}
          data-foto={product.fotoUrl}
          data-moneda={product.moneda}
        >
          Agregar
        </button>
      )}
    </div>
  </div>
</article>

<style>
  .card {
    border: 1.5px solid #111;
    border-radius: 6px;
    overflow: hidden;
    background: var(--surface-color, #fff);
    display: flex;
    flex-direction: column;
  }
  .foto {
    position: relative;
  }
  .foto-zoom {
    display: block;
    width: 100%;
    border: none;
    margin: 0;
    padding: 0;
    background: none;
    cursor: zoom-in;
  }
  .foto img {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: contain;
    display: block;
    background: #f2f2f2;
  }
  .estado {
    position: absolute;
    top: 0.5rem;
    left: 0.5rem;
    font-size: 0.68rem;
    padding: 0.2rem 0.55rem;
    border-radius: 3px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }
  .estado-ok {
    background: #111;
    color: #fff;
  }
  .estado-poco {
    background: #fff;
    color: var(--brand-color, #e10600);
    border: 1.5px solid var(--brand-color, #e10600);
  }
  .estado-agotado {
    background: var(--brand-color, #e10600);
    color: #fff;
  }
  .info {
    padding: 0.65rem 0.75rem 0.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    flex: 1;
  }
  .categoria {
    margin: 0;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--secondary-text-color, #888);
  }
  .info h3 {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.25;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.01em;
  }
  .fila-inferior {
    margin-top: auto;
    padding-top: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.5rem;
  }
  .precio {
    margin: 0;
    font-weight: 700;
    font-size: 1.05rem;
  }
  .btn-agregar {
    border: none;
    background: var(--brand-color, #e10600);
    color: #fff;
    font-weight: 700;
    font-size: 0.82rem;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    padding: 0.4rem 0.75rem;
    border-radius: 4px;
    flex-shrink: 0;
  }
</style>
